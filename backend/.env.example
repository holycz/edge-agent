# AI 服务配置 - 请复制此文件为 .env 并填写真实值
# cp .env.example .env

# API Key（必填）- 不要将真实 key 提交到 git
API_KEY=your_api_key_here

# API URL（可选，默认值如下）
API_URL=https://integrate.api.nvidia.com/v1

# 模型名称（可选）
MODEL=qwen/qwen3-next-80b-a3b-instruct

# 服务端口（可选）
PORT=8765
